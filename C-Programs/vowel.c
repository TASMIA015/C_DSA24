// day1
#include<stdio.h>
void main()
{
    char ch='I';
    if (ch == 'a' || ch == 'e' || ch == 'o'|| ch == 'u' || ch == 'u' )
        puts("it is a vowel.....");
    
    else
        puts("not a vowel.....");
    
}
